================================================================================
SOU+BLU — DEPLOY COMPLETO (pasta REMONTADO → public_html Locaweb)
Atualizado: 2026-06-22
Site: https://www.soumaisblu.com.br
================================================================================

IMPORTANTE
----------
- Apague o conteúdo de public_html/ e envie ESTA pasta inteira.
- ANTES de apagar: baixe do servidor a pasta uploads/proposal-attachments/
  (há ~42 arquivos reais no disco Locaweb em 22/06/2026). Se apagar sem
  backup, esses anexos somem — só voltam com backup do painel Locaweb.
  Alternativa: suba REMONTADO mas NÃO apague uploads/ no servidor (só mesclar).
- NÃO envie a subpasta SOUBLU_deploy_2026-06-19/ (backup antigo).
- Inclua seus arquivos locais de config (não estão no git):
    config.db.local.php
    config.supabase.local.php
    config.pix.local.php (se PIX ativo)

================================================================================
1. CONFIGS OBRIGATÓRIOS NA RAIZ DO public_html
================================================================================

config.db.local.php (OBRIGATÓRIO)
  DB_HOST, DB_NAME, DB_USER, DB_PASS
  API_INTERNAL_KEY  — igual ao API_KEY no js/db-connect.js
  SITE_URL          — https://www.soumaisblu.com.br
  UPLOAD_DIR        — __DIR__ . '/uploads'

config.supabase.local.php (OBRIGATÓRIO para anexos)
  SUPABASE_URL, SUPABASE_ANON_KEY
  SUPABASE_SERVICE_KEY  — service_role legado (obrigatório para reparo/upload PHP)
  SUPABASE_V2_URL, SUPABASE_V2_ANON_KEY
  SUPABASE_V2_SERVICE_KEY — opcional se buckets v2 forem só públicos

config.pix.local.php (se usar saque PIX)
  PIX_DB_BACKEND = mysql, credenciais Efi, etc.

Verifique no servidor após upload:
  - Pasta uploads/ gravável (755 ou 775)
  - uploads/whatsapp-media/ existe (estrutura vazia OK)
  - api/.htaccess presente

================================================================================
2. O QUE SUBIR (estrutura principal)
================================================================================

  index.html, admin.html, employee.html, financeiro.html, rh-manager.html, ...
  pages/*.html
  js/   css/   images/   api/   uploads/.htaccess   certs/
  config.*.local.php (seus arquivos reais, não os .example)
  .htaccess (raiz)

NÃO subir:
  SOUBLU_deploy_2026-06-19/
  scripts/ (opcional — só na sua máquina para migração CLI)
  .git/

================================================================================
3. VERSÕES JS (cache-bust pós-deploy)
================================================================================

Após upload, todos os painéis devem carregar:
  proposals.js?v=58
  db-connect.js?v=1781813000
  auth.js?v=1781813000
  tickets.js?v=1781813000
  whatsapp-chat.js?v=9

Se o navegador ainda mostrar anexo quebrado: Ctrl+F5 (ou limpar cache).

================================================================================
4. PÓS-DEPLOY — REPARO DE ANEXOS (MySQL)
================================================================================

A) Via navegador/curl (no servidor ou seu PC):

  # Simular (não grava)
  curl -s "https://www.soumaisblu.com.br/api/repair-proposal-attachments.php?dry_run=1&limit=50" ^
    -H "X-API-Key: SEU_API_INTERNAL_KEY"

  # Executar lote (gravar URLs corrigidas) — use limit=20 para evitar 504
  curl -s -X POST "https://www.soumaisblu.com.br/api/post-deploy.php" ^
    -H "X-API-Key: SEU_API_INTERNAL_KEY" ^
    -H "Content-Type: application/json" ^
    -d "{\"limit\":20,\"offset\":0}"

  Repita aumentando offset até has_more=false na resposta JSON.

B) Via PowerShell local (conecta MySQL remoto + Supabase):

  cd C:\Users\bluno\Downloads\leadsmanager_atualizado\public_ht_REMONTADO
  php scripts/migrate-attachments-cli.php --dry-run
  php scripts/migrate-attachments-cli.php

C) Restauração local (backup public_html (32) — 27 anexos já migrados em 22/06):
  powershell -ExecutionPolicy Bypass -File .\scripts\restore-missing-attachments.ps1 -Copy -Migrate -LocalOnly
  Inclua uploads/proposal-attachments/ no deploy (não apague no servidor).

D) Script auxiliar reparo pós-deploy:
  .\scripts\repair-attachments.ps1 -DryRun
  .\scripts\repair-attachments.ps1

================================================================================
5. TESTES RÁPIDOS
================================================================================

1) Login em https://www.soumaisblu.com.br
2) Financeiro → abrir proposta com anexo antigo → miniatura e PDF abrem
3) Admin → supervisor → abrir chamado (formulário visível)
4) api/file.php?path=proposal-attachments/...&check=1 → JSON ok:true
5) Ctrl+F5 em admin, employee e financeiro

================================================================================
6. ANEXOS — O QUE ESPERAR
================================================================================

Migração CLI já executada (jun/2026):
  ~928 já no Supabase (abrem normalmente)
  ~12 migrados para Supabase (CLI local)
  ~381 MISSING no CLI — mas verificação Locaweb (22/06):
      ~35–42 arquivos AINDA existem no disco (path aninhado img_*.pdf/hash.pdf)
      ~340+ realmente ausentes → só reenvio ou backup painel Locaweb

Após deploy do FileStorage.php novo + reparo em lotes (limit=20), parte dos
que hoje falham por 403/path errado no MySQL pode migrar para Supabase.

Percentual estimado que ABRE após deploy + reparo:
  ~71–75% dos anexos com URL gravada; restante = reenvio manual.

api/file.php tenta nesta ordem:
  1) Supabase (legado + v2, público e autenticado)
  2) Disco /uploads/ local
  3) Reparo: envia local → Supabase se achar arquivo no disco

================================================================================
7. SUPORTE
================================================================================

Logs: resposta JSON de repair-proposal-attachments.php (stats.missing)
Anexo novo: sempre vai ao Supabase (js/db.js uploadProposalFile)
Documentação anexos: DEPLOY-ANEXOS-2026-06-22.txt

================================================================================
