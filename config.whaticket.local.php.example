<?php
/**
 * Configuração WhaTicket Community (motor WhatsApp no VPS).
 * Copie para config.whaticket.local.php na raiz do site e preencha.
 */
declare(strict_types=1);

/** evolution | zapi | whaticket */
define('WHATSAPP_PROVIDER', 'whaticket');

define('WHATICKET_ENABLED', true);
define('WHATICKET_API_URL', 'https://wt-api.seudominio.com');
define('WHATICKET_API_TOKEN', 'token-gerado-no-whatticket');
define('WHATICKET_WHATSAPP_ID', '1');

/** Bridge Socket.IO → QR cache + webhook (mesmo VPS). */
define('WHATICKET_BRIDGE_URL', 'https://wt-bridge.seudominio.com');

/** Segredo compartilhado bridge → PHP (?secret=). */
define('WHATICKET_WEBHOOK_SECRET', 'segredo-compartilhado-bridge');

define('WHATICKET_SSL_VERIFY', true);
