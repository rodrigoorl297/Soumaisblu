<?php
/**
 * PRODUÇÃO (Locaweb public_html) — copie para config.db.local.php na raiz do site.
 * Mesma pasta que api/, index.html e config.supabase.local.php.
 * NÃO commitar o arquivo real (contém senha MySQL).
 */

define('DB_HOST', 'soumaisblu.mysql.dbaas.com.br');
define('DB_NAME', 'soumaisblu');
define('DB_USER', 'soumaisblu');
define('DB_PASS', 'sua_senha_mysql');
define('DB_CHARSET', 'utf8mb4');

/**
 * Chave usada pelo navegador e pelo PHP (header X-API-Key).
 * Deve ser IGUAL a SOUBLU_CONFIG.API_KEY em js/hostinger-connect.js / js/db-connect.js
 */
define('API_INTERNAL_KEY', 'soublu_api_52e8c7a6b3df4019');

/** URL pública do site (sem barra final) — usada em links de anexo e api/file.php */
define('SITE_URL', 'https://www.soumaisblu.com.br');

/** Pasta de uploads no servidor (deve existir e ser gravável: chmod 755 ou 775) */
define('UPLOAD_DIR', __DIR__ . '/uploads');
