<?php
/**
 * PRODUÇÃO (Locaweb public_html) — copie para config.supabase.local.php na raiz.
 * NÃO commitar (está no .gitignore).
 *
 * PROJETO ORIGINAL (sou+blu / dqptnlywbarvznpzgtuj):
 *   propostas, crédito, anexos, dados gerais
 *
 * PROJETO soublu-v2 (cpqediswbjxcvpnwflyj):
 *   SOMENTE WhatsApp (quando WA_DB_BACKEND=supabase)
 */

/** Original — propostas, anexos, storage principal (guards evitam aviso se config.pix.local.php já definiu SUPABASE_*) */
if (!defined('SUPABASE_URL')) {
    define('SUPABASE_URL', 'https://dqptnlywbarvznpzgtuj.supabase.co');
}
if (!defined('SUPABASE_ANON_KEY')) {
    define('SUPABASE_ANON_KEY', 'COLE_ANON_LEGADO');
}
if (!defined('SUPABASE_SERVICE_KEY')) {
    define('SUPABASE_SERVICE_KEY', 'COLE_SERVICE_ROLE_LEGADO');
}

if (!defined('SUPABASE_LEGACY_URL')) {
    define('SUPABASE_LEGACY_URL', SUPABASE_URL);
}
if (!defined('SUPABASE_LEGACY_ANON_KEY')) {
    define('SUPABASE_LEGACY_ANON_KEY', SUPABASE_ANON_KEY);
}
if (!defined('SUPABASE_LEGACY_SERVICE_KEY')) {
    define('SUPABASE_LEGACY_SERVICE_KEY', SUPABASE_SERVICE_KEY);
}

/** soublu-v2 — APENAS WhatsApp */
if (!defined('SUPABASE_V2_URL')) {
    define('SUPABASE_V2_URL', 'https://cpqediswbjxcvpnwflyj.supabase.co');
}
if (!defined('SUPABASE_V2_ANON_KEY')) {
    define('SUPABASE_V2_ANON_KEY', 'COLE_ANON_V2');
}
if (!defined('SUPABASE_V2_SERVICE_KEY')) {
    define('SUPABASE_V2_SERVICE_KEY', 'COLE_SERVICE_ROLE_V2');
}

/** Propostas de crédito ficam no MySQL Locaweb (tabela proposals) */
define('WA_DB_BACKEND', 'mysql');
